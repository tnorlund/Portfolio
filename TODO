# Receipt Trainer

[x] Update `_load_dynamo_data` to use the correct output from `listReceiptDetails`

- [x] Update GSI2 PK and SK so that only **Receipt**, **Receipt Word**, and **Receipt Word Label** use GSI2 PK = `RECEIPT`
