{
  "mcpServers": {
    "ats-verification": {
      "type": "http",
      "url": "${ATS_MCP_URL}",
      "auth": {
        "CLIENT_ID": "${ATS_MCP_CLIENT_ID}",
        "scopes": [
          "openid",
          "email",
          "portfolio-mcp/ats"
        ]
      }
    }
  }
}
