# Handoff: state of play — 2026-08-04 (read-only census)

## 1. Merged since 2026-08-01 (22 PRs; origin/main @ 1f790a2f8)
Line-items core: **#1324** three-figure recon baseline + graded match · **#1329** zone-gap ITEMS boundary extension · **#1331** agentic review loop Phases 0-3 (is_proven, triage, adjudicate, writer) · **#1332** writer honors freeze markers, re-checked before every write · **#1334** writer confirm loop, stale-stream vs true divergence · **#1349** tender rows never anchor printed grand total (`TENDER_KEYWORD_RE` in `receipt_dynamo/amounts.py`) + writer re-run idempotency · **#1351** first agent-originated golden promotions (2 Sprouts receipts, user-signed-off).
Re-OCR: **#1350** SMART re-OCR — strategy ladder (`plain|invert|deskew|upscale2x`), OCRJob contract fields (`reocr_strategy`, `reocr_mechanism`, `reocr_words_accepted/rejected`, `reocr_delta_before/after`), outcome harvest. **Python half only — the Swift consumer PR never landed.**
Infra/ops: #1327 reseg apply hardening · #1328 site-bucket scoped `sync --delete` · #1318 CDN re-warp scan/repair · #1323 mcp pin `<2` (prod cold-start outage) · #1330 docs: state + forward plan · #1333 dependabot coverage. Plus #1292–1297, #1325, #1326 dependency bumps.

## 2. Open PRs / un-merged work
17 open PRs — **15 are dependabot** (#1335–#1348), plus #1298 address-similarity VALID filter and #1290 Swift chroma shadow pipeline (draft), #1260 render regression guard. **Nothing open covers line-items or re-OCR.**
**The review harness is un-merged.** `origin/codex/geometric-reader` is +7 commits / 35 files / ~10k lines ahead of main and carries the entire workstation: `portfolio/dev-harness/validation_shim.py` (1708 lines), `pages/dev/validation.tsx`, `VALIDATION_MATH_AUDIT.md`, the GeometricReader figure, and the Phase-4 three-screen shrink (08b303f5e). Portfolio_golden2 is checked out on this branch with 7 dirty/untracked files. No PR exists for it.

## 3. DEV — ReceiptsTable-dc5be22 (821 receipts, 821 summaries)
Recon: **match 480 · none 168 · mismatch 73 · no-baseline 57 · near 43**
**PROVEN (match + cent-exact bank, `line_items.geometry.is_proven`): 281 / 821 = 34.2%**
Tender/bank coverage: tender_class 813 (card 718 / unknown 68 / cash 27) · ledger 655 · card_last4 585 · bank_amount 425 · grand_total 714
Golden set: **33** receipts on the checked-out branch, **35** on origin/main (218 true_items; Sprouts 8, Home Depot 3, Amazon Fresh / Costco / Gelson's / In-N-Out 2 each). #1351's two promotions are on main but not on golden2's branch.

## 4. PROD — ReceiptsTable-d7ff76a (822 receipts, 822 summaries)
Recon: **match 487 · none 122 · mismatch 119 · near 48 · no-baseline 46**
**PROVEN: 298 / 822 = 36.3%**
Tender/bank: tender_class 822 (card 728 / unknown 68 / cash 26) · ledger 665 · card_last4 601 · bank_amount 439 · grand_total 778

**Divergence — prod is AHEAD of dev, which inverts the usual direction.** Prod has 17 more PROVEN receipts and **64 more summaries carrying a grand_total (778 vs 714)**. Dev holds 46 more receipts at `none` recon (168 vs 122) while prod holds 46 more at `mismatch` (119 vs 73) — the same 46 receipts appear to have lost their ITEMS/figures in dev, not gained a verdict. Dev is where the agentic writer has been running; treat the dev deficit as writer-caused erosion to be explained before any dev→prod promotion, and do not assume dev is the newer truth.

## 5. Re-OCR system — shipped but never ran
`reocr_strategy` is written by zero jobs in either table: **0 of 481 dev REGIONAL_REOCR jobs and 0 of 153 prod jobs carry the field** — exactly as expected, since #1350 landed the Python contract and the Swift preprocessing half was never opened as a PR.
- **Dev** (dc5be22): 1587 OCR jobs (FIRST_PASS 738 / REFINEMENT 368 / REGIONAL_REOCR 481). 26 REGIONAL_REOCR created since 08-03: **16 COMPLETED, 10 PENDING**. Latest created 2026-08-03T23:59Z.
- **Prod** (d7ff76a): 175 OCR jobs (REGIONAL_REOCR 153 / FIRST_PASS 22). 64 created since 08-03 and **all 64 are still PENDING — nothing has ever drained the prod queue.** Latest created 2026-08-03T19:26Z.
- **Local runner (MacBook Pro): installed but never started.** `~/Library/LaunchAgents/com.tnorlund.receipt-ocr-dev.plist` exists (calendar :00/:15/:30/:45, `KeepAlive=false`, `RunAtLoad=false`, runs `~/receipt_ocr_runner/run-dev.sh` which is present and executable). But `launchctl list | grep -i receipt` returns nothing — the job was never `launchctl load`ed — and `~/Library/Logs/receipt-ocr-dev.log` **does not exist**, so it has never produced a single run. Not draining.
- **Mac mini: nothing installed.** ssh shows no launchd job, no plist in `~/Library/LaunchAgents/`, no log, and no `~/receipt_ocr_runner/` directory at all. The interleaved :07/:22/:37/:52 schedule the dev plist's comment describes was never built.
- Net: 74 re-OCR jobs (10 dev + 64 prod) are queued with no consumer on either machine.

## 6. `.dev-harness/` in Portfolio_golden2
dossiers **207** (`dossier-v2`; each carries recon, bank, visual_evidence, verdict_recommendation, confidence + justification, abstain_reason) · verdicts **8** · approvals **3** · queues **1** · `review_log.jsonl` **0 bytes / 0 lines — never written to**. All timestamped 2026-08-03; the 207 dossiers cover only a quarter of dev's 821 receipts and are a stale snapshot predating #1349's tender-anchor fix.
