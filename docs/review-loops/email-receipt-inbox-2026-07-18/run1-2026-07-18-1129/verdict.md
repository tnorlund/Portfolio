1. [med] `infra/email_receipt_inbox/lambdas/handler.py:204` — Runtime parser defects raising `TypeError`, `AttributeError`, or other broadly caught exceptions are still persisted as successful `parse_error` results, bypassing retries and the DLQ. Cold-start validation cannot detect failures inside parser bodies. Fix: catch only a dedicated per-message parsing exception; allow all unexpected parser exceptions to propagate.
