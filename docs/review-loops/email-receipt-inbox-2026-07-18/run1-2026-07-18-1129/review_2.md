1. [med] `infra/email_receipt_inbox/infrastructure.py:83-90,112-135,258-265` — The MX record and active rule set can be created before bucket versioning and notifications. Mail arriving in that window is stored unversioned and may never trigger parsing; lifecycle configuration can likewise race versioning. Retain the resource handles, make lifecycle depend on versioning, make activation depend on versioning and notification, and publish MX only after activation.

2. [med] `infra/email_receipt_inbox/lambdas/handler.py:90-105,141-146` — Version-aware reads still write every version of a raw key to the same `parsed/<name>.json`. Because S3 notifications are unordered, an older replay can become the current parsed object, while the documented downstream `message_id` dedupe remains sender-controlled. Key parsed objects by `ingest_id` or raw `versionId`, and require downstream idempotency on `ingest_id`.

3. [med] `infra/email_receipt_inbox/lambdas/handler.py:112-127`, `registry.py:61-64`, and multi-domain parser entry points such as `parse_retail.py:56-59,270-274`, `parse_services.py:301-315`, and `parse_pos_restaurants.py:373-390` — The handler selects a group using `X-Original-From`, but passes the unchanged message to parsers that re-dispatch using rewritten `From`. In the documented iCloud-forwarding case, these parsers silently return empty/non-receipt results. Pass canonical sender metadata into registry adapters or normalize sender dispatch consistently without changing the original evidence.

4. [med] `infra/email_receipt_inbox/lambdas/handler.py:66-80,115-118` — Content scanning fails open for `GRAY`, `PROCESSING_FAILED`, or missing verdicts; only literal `FAIL` is rejected. SES explicitly reports these indeterminate/failure states, including malformed MIME that it could not scan. Quarantine at least non-`PASS` virus verdicts and `PROCESSING_FAILED` spam verdicts, preferably using receipt-action metadata rather than message headers. [AWS verdict statuses](https://docs.aws.amazon.com/ses/latest/dg/receiving-email-notifications-contents.html)

5. [low] `infra/email_receipt_inbox/infrastructure.py:136-151` — The SES bucket grant is scoped only by account, allowing any SES receipt rule in that account to write under `raw/`. Give the rule set and rule deterministic physical names and add the corresponding `AWS:SourceArn` condition recommended for SES S3 delivery. [AWS SES permission example](https://docs.aws.amazon.com/ses/latest/dg/receiving-email-permissions.html)

6. [deadlock] `infra/email_receipt_inbox/lambdas/handler.py:45-80,112-133` — The public-ingress trust issue remains: arbitrary `X-Original-From` values drive parser selection, while authentication results are merely recorded and receipts/signals are still emitted. Preserve forwarding by requiring verifiable provenance from an explicitly trusted forwarder, but quarantine direct or forwarded messages that do not satisfy that policy before populating `receipt`.

7. [deadlock] `infra/email_receipt_inbox/lambdas/parsers/parse_costco.py:100-109` and `registry.py:80-86` — The prior decline says missing `pypdf` routes Costco PDFs to OCR, but the deployed branch only adds `_note`; it does not set `needs_ocr`. Consequently the gas ticket becomes `non_receipt` and its metadata is discarded. Package `pypdf`, or split the condition and set `needs_ocr=True` when a PDF exists but `PdfReader` is unavailable.
