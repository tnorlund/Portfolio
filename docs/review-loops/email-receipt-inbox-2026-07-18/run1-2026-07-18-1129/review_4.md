1. [high] `infra/email_receipt_inbox/lambdas/handler.py:164` — `_content_rejected` expects `(spam, virus)`, but the call passes `(virus, spam)`. This allows missing/`GRAY` virus verdicts through while quarantining benign missing/`GRAY` spam verdicts. Call it with explicit keywords: `_content_rejected(spam=out["spam_verdict"], virus=out["virus_verdict"])`, and test the verdict matrix.

2. [med] `infra/email_receipt_inbox/lambdas/registry.py:84-89` — Venmo P2P mail becomes `txn_signal` solely from `transaction_kind`, even when parsing produced no amount or usable direction. Subject-only matches such as iMessage notifications can therefore emit unusable reconciliation signals. Require a non-null `grand_total` and valid `direction`, as done for Chase, otherwise classify as `non_receipt`.

3. [low] `infra/email_receipt_inbox/infrastructure.py:282-308` — `activate=False` skips rule-set activation but still publishes the public MX record. Mail will then be evaluated against some other active rule set or rejected instead of reaching this rule. Only create the MX record when this rule set is activated, or require an explicitly supplied active-set integration before publishing it.
