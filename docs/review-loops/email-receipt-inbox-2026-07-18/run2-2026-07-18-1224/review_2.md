1. [med] `infra/email_receipt_inbox/infrastructure.py:116-120,315-348` — `depends_on` does not cover S3’s first-time versioning propagation window. AWS warns that propagation can take 15 minutes and newly written objects may return `NoSuchKey`; activation/MX can therefore admit mail whose immediate parser reads fail or whose version lineage is unreliable. Add a 15-minute barrier keyed to the bucket/versioning resource before activating the rule set, or require a two-phase deployment after the documented wait. [AWS S3 guidance](https://docs.aws.amazon.com/AmazonS3/latest/userguide/manage-versioning-examples.html)

2. [med] `infra/email_receipt_inbox/lambdas/handler.py:198-201` — `needs_ocr` is derived from parser output, but that output is discarded because `receipt` is populated only for `receipt` and `txn_signal`. Apple PDF receipts consequently lose already extracted fields such as attachment name, date, and order ID. Persist the parser result for `needs_ocr` as well, either in `receipt` or a separate `parser_output` field.
