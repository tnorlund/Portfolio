1. [high] `infra/email_receipt_inbox/lambdas/handler.py:57-92,161-189` — sender authentication is recorded but never enforced. Any public sender can spoof `From: ...@chase.com`, or supply the completely untrusted `X-Original-From`, and produce a `receipt`/`txn_signal` if spam and virus scans pass; SES does not reject mail based on SPF/DKIM/DMARC itself. Require an SES-generated DMARC PASS aligned with the domain used for registry dispatch, and never trust `X-Original-From` without validated forwarding/ARC provenance. Prefer a synchronous SES Lambda action before S3 that rejects unauthenticated or unapproved senders using the trusted receipt verdicts. [SES authentication behavior](https://docs.aws.amazon.com/ses/latest/dg/receiving-email-concepts.html)

2. [med] `infra/email_receipt_inbox/infrastructure.py:218-220`, `infra/email_receipt_inbox/lambdas/parsers/parse_costco.py:26-29,100-109` — the Lambda artifact contains only `LAMBDA_DIR`, but the Costco integration requires third-party `pypdf`, which is neither vendored nor attached through a layer. The managed Python runtime supplies Boto3, not arbitrary packages; consequently `PdfReader` becomes `None`, valid Costco gas receipts silently classify as `non_receipt`, and their parser output is discarded. Package a pinned `pypdf` dependency in the deployment archive/layer and test the built artifact; alternatively mark this fallback `needs_ocr` so it remains actionable. [Lambda dependency packaging](https://docs.aws.amazon.com/lambda/latest/dg/python-package.html)

3. [low] `infra/email_receipt_inbox/lambdas/handler.py:228-238`, `infra/email_receipt_inbox/infrastructure.py:116-142` — the claimed idempotent write is an unconditional `PutObject` into a versioned bucket. Every duplicate S3 notification, Lambda retry, or replay creates another parsed-object version, retained indefinitely by default. Add a parser/schema revision to the destination identity and use `IfNoneMatch="*"` for normal writes, treating HTTP 412 as a successful duplicate; deliberate reparsing can then use a new revision. [S3 conditional writes](https://docs.aws.amazon.com/AmazonS3/latest/userguide/conditional-writes.html)
