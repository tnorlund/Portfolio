"""Similarity evaluation harness for the Chroma removal (Round A).

Captures golden fixtures from live Chroma Cloud (``capture_golden.py``) and
grades any ``VectorSearchClient`` backend against them (``evaluate.py``).
"""
