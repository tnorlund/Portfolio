import React from "react";
import DockIcon from "./DockIcon";

/** The Claude app icon, doing its Dock launch bounce. */
const ClaudeAppLogo: React.FC = () => (
  <DockIcon src="/claude-icon.svg" alt="Claude app icon" />
);

export default ClaudeAppLogo;
