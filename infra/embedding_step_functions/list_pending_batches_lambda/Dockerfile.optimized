
# Use the base image with all dependencies pre-installed
ARG BASE_IMAGE
FROM ${BASE_IMAGE}

# Copy the handler code (context is the embedding_step_functions directory)
COPY list_pending_line_batches_for_polling.py ${LAMBDA_TASK_ROOT}/

# Command to run the Lambda handler
CMD ["list_pending_line_batches_for_polling.poll_handler"]
