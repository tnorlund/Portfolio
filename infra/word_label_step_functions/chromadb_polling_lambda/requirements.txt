# ChromaDB and dependencies - match receipt_label requirements
chromadb>=0.5.0
numpy>=1.24.0

# AWS and other dependencies
boto3>=1.34.0
openai>=1.0.0
pydantic>=2.0.0

# Note: Local packages (receipt_label, receipt_dynamo) are installed
# directly in the Dockerfile to avoid editable install issues