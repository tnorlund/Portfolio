import pytest
from receipt_label.submit_completion_batch import submit_batch


@pytest.mark.integration
def test_submit_completion_batch(
    # dynamodb_table_and_s3_bucket: tuple[str, str],
    # receipt_words: list[ReceiptWord],
    # mocker,
):
    pass
